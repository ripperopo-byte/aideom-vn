services:
  web-dashboard:
    build: .
    container_name: aideom_streamlit_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      STREAMLIT_SERVER_HEADLESS: "true"
      STREAMLIT_SERVER_PORT: 8501
    command: streamlit run app.py